# Overview

FlexibleStake is a multi-pool staking contract that allows independent staking and reward configurations for multiple tokens.
Each pool defines its own rate, duration, and TVL cap, with optional platform-token rewards.
It supports fee-on-transfer tokens, auto-compounding reward updates, pause control, and safe withdrawals — making it a key component in the reward infrastructure of the DeFi ecosystem.